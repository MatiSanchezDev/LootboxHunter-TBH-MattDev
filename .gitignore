# PyInstaller
build/
dist/
dist_onedir/
*.spec
*.zip

# Binarios: se distribuyen via GitHub Releases, no en el repo
/LootboxHunter.exe

# Python
__pycache__/
*.pyc

# App local data (por las dudas, no deberia estar en el repo)
config.json

# Tooling / agentes (no es parte del proyecto)
.atl/
.claude/
